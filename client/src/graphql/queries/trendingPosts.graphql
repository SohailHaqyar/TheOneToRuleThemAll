query TrendingPosts {
  findAllTrendingPosts {
    id
    created_at
    title
    body
    comments {
      id
      body
      user {
        id
        username
        imageUrl
      }
    }
    likes {
      id
    }
    user {
      username
      imageUrl
      id
    }
  }
}
